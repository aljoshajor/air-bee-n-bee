
<div class="container">
 <div class="card card-bee mb-3" style="max-width: 800px;">
    <div class="row no-gutters">
      <div class="col-md-4">
        <img src="<%= image_path("#{@bee.image}") %>" class="card-img" alt="Card image cap">
      </div>
      <div class="col-md-8">
          <div class="card-body">
            <h1><%= @bee.name %> is a great bee!</h1>
            <h5> <%= @bee.address %> </h5>
            <h4>owned by <strong><%= @bee.user.name.capitalize %></strong></h4>
            <p><%= @bee.user.name.capitalize %> has described <%= @bee.name.capitalize %> as a <%= @bee.features.downcase %> bee and it is clear to see why! From producing honey to disarming landmines you can "bee" sure that <%= @bee.name.capitalize %> will do it in a <%= @bee.features.downcase %> way!</p>
            <h5> Popularity: <%= @bee.name %> received <%= @bee.likes.count %> <%= (@bee.likes.count) == 1 ? '♥︎' : '♥︎'%> favourites and <%= @bee.bookings.count %> <%= (@bee.bookings.count) == 1 ? "booking" : 'bookings' %>  so far!</h5>
            <br>
             <%= link_to 'Book me', new_bee_booking_path(@bee), class: "btn btn-primary" %> <%= button_to '♥︎ Bee', bee_likes_path(@bee), method: :post, class: "btn btn-primary" %>
        </div>
     </div>
    </div>
  </div>

<br>

<ul class="list-group">
  <% @bee.bookings.each do |booking| %>
  <% if booking.accepted == false %>
  <li class="list-group-item ">A booking has been requested between <%= booking.start_date %> and <%= booking.end_date %> by <%= booking.user.name %> <%= link_to "✅ Confirm", booking_path(booking), class: "btn btn-primary", method: :patch, data: { confirm: "Confirm booking with #{booking.user.name}? "} %> </li>
  <% else %>
  <li class="list-group-item ">A booking has been confimed between <%= booking.start_date %> and <%= booking.end_date %> with <%= booking.user.name %> <%= link_to "❌ Cancel", booking_path(booking), class: "btn btn-danger", method: :patch, data: { confirm: "Cancel booking with #{booking.user.name}? "} %> </li>
  <% end %>
  <% end %>
</ul>
</div>


